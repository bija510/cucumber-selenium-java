package Base;

import org.openqa.selenium.WebDriver;

public class BaseUtil {
	
	public WebDriver driver;
	
	/******************************************************************
	 * WebDriver is not the Class
	 * WebDriver is a Interface which extends with
	 * SearchContext[Control + click on WebDriver to see]
	 *******************************************************************/
	
	
}
